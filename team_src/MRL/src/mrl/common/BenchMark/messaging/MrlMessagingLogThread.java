/*
package mrl.common.BenchMark.messaging;

import Manager.LogManager;

import java.util.List;

*/
/**
 * @author Mahdi
 *//*

public class MrlMessagingLogThread extends Thread {
    private List<LogManager> logManagers;

    public MrlMessagingLogThread(List<LogManager> logManagers) {
        this.logManagers = logManagers;
    }

    @Override
    public void run() {
        for (LogManager logManager : logManagers) {
            logManager.execute();
        }
    }

}
*/
